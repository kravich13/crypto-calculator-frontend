import { PageLayout } from '@cc/app/components';
import { useEmailValidateMutation } from '@cc/shared/api';
import { RoutesTypes } from '@cc/shared/enums';
import { useAuthContext, useErrorMessage, useNotAuthPage } from '@cc/shared/lib';
import globalStyles from '@cc/shared/styles/Index.module.css';
import { Container, Typography } from '@mui/material';
import Head from 'next/head';
import { useRouter } from 'next/router';
import { useEffect } from 'react';

interface IRouterQuery {
  code?: string;
  email?: string;
}

const ConfirmEmail = () => {
  const { showContent } = useNotAuthPage({ redirectTo: RoutesTypes.MAIN });
  const { login } = useAuthContext();

  const query = useRouter().query as IRouterQuery;
  const [emailValidate, { isError, data, error }] = useEmailValidateMutation();

  const errorMessage = useErrorMessage(error).includes('Code lifetime expired')
    ? 'Code lifetime expired.'
    : '';

  const forUserText = Boolean(errorMessage) ? 'Login again' : 'Please try again later';

  useEffect(() => {
    if (query.code && query.email) {
      emailValidate({ code: query.code, email: query.email });
    }
  }, []);

  useEffect(() => {
    if (showContent && data) {
      login({ tokensData: data, notifyUser: true, redirectTo: RoutesTypes.MAIN });
    }
  }, [showContent, data]);

  return (
    <PageLayout centerContent>
      <Head>
        <title>Calculator | Confirm email</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      {showContent && isError && (
        <Container maxWidth="xs" className={globalStyles.opacityContainer}>
          <Typography variant="h6" component="h1" color="red" fontWeight="600">
            An error has occurred
          </Typography>

          {Boolean(errorMessage) && <Typography sx={{ mt: 2 }}>Reason: {errorMessage}</Typography>}

          <Typography sx={{ mt: 2 }}>{forUserText}</Typography>
        </Container>
      )}
    </PageLayout>
  );
};

export default ConfirmEmail;
