import { SxProps } from '@mui/material';

export const globalPageStyles = {
  width: '100%',
  height: '100%',
  display: 'flex',
  flexDirection: 'column',
  paddingTop: 3,
  paddingBottom: 3,
} as SxProps;
